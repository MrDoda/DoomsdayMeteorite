# Doomsday Meteorite

## Installation

To install all the required dependencies for the Doomsday Meteorite project, follow these steps:

1. Open a terminal or command prompt.
2. Navigate to the project's root directory where the `requirements.txt` file is located.
3. Run the following command:

```sh
pip install -r requirements.txt
```
